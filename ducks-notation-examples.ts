
type MapMenuPrintTimecardDispatch = any;

export function mapDispatchCreator(validationService, positionsService) {
	return (dispatch: ((arg) => void)): MapMenuPrintTimecardDispatch => ({
		savePosition: (): void => {

		}
	});
}