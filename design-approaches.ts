/*
смысл переизобретать редакс на rxJS???

1. Use redux
  - pros:
  - cons:

2. Use AngularJS(unidirectional)
  - pros:
  - cons: no single source of truth, mess, DI problems, mutable scope, parent accessing etc.


3. Use RxJS (single store)
  - pros:
  - cons: 
      reimplement selectors, when call next() like dispatch,
      logic will be in different place (hard to support, maintain, extend)


4. Use RxJS (multiple stores)
  - pros:
  - cons:

*/
